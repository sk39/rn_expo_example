import {action, observable} from "mobx";

/**
 * TODO: Global state.
 */
export default class RootStore {

    // @observable tmp: boolean = false;
    //
    // @action
    // set(tmp) {
    //     this.tmp = tmp;
    // }
}

